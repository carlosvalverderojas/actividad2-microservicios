server:
  port: ${PORT:8761}   # Render usa PORT dinámico, local cae en 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
